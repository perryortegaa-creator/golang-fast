services:
  db:
    image: mysql:8
    container_name: local-mysql-fast-campus
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: superSecret
      MYSQL_DATABASE: belajarfast
    volumes:
      - ./db:/var/lib/mysql